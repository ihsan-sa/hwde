# P5 Board Setup - T6 evaluation (2026-08-06)

Sources: eval JSON (whole-stage lens: board_init.py, rules_gen.py, lib/fabfloors.py,
lib/impedance.py, stackups.yaml, jlc_capabilities.yaml, agents/board-setup.md, bench P5).
Applied via Batch E (commit f48197d) and Batch K (commit ad2576c).

## Verdict
- correctness 7: post-T1 core verified (fabfloors single-source, stackup refusal, 48/48
  tests) but one silent wrong-answer path survived - rules_gen's --copper-oz default 1.0
  ignored the passed stackup, live-reproduced this session.
- knowledge_level 6.5: HV clearance (voltages), V12 stripline, V18 churn all sat at L0/L1
  prose; carrier hand-authored its 48V DRU rules.
- output_quality 8: shipped DRU/pro artifacts correct on all four boards; gaps were latent,
  not shipped defects (dodged by operator knowledge).
- token_cost 9 / wall_time 9: 2 deterministic scripts, board_init live leg 2.86s; the
  board-setup agent is pure transcription.

## Findings
- F1 copper-oz trap: rules_gen.py:342 default=1.0 + profile() called before stackup load.
  Live repro: --layers 2 --stackup JLC2313_1.6_2oz -> capability_class 2layer_1oz, VBUS
  3.5mm (sub-fab 0.127 floor vs 0.1524 needed); check_pro passed because it asserted the
  same wrong row. board_init had the T1 fix (board_init.py:328-332); rules_gen did not.
- F2 agent prompt reaches F1: agents/board-setup.md:38-40 step-3 template omitted
  --copper-oz entirely - verbatim following triggers F1 on every non-1oz board.
- F3 rules_gen never read constraints.voltages (grep: zero matches); carrier's 48V rules
  hand-written into the DRU while check_creepage.py:54-100 already carried IPC-2221 6-1.
- F4 (V12): impedance.py outer-microstrip only; nothing prevented routing a solved net on
  an inner layer (stackups.yaml:44, PROGRESS.md S8 item 4). Latent, zero occurrences.
- F5 (V18): JLC template churn measured (7628G gone in <7 days) but provenance.verified
  never re-checked by any script (ladder row 169).
- F6 bench blind spot: score_p5 (bench.py:224-260) ran board_init only - the whole T1
  rules_gen surface (netclass split, IPC-2152 widths, floor severities) un-benched.
- F7 (row 160): hand-edited .kicad_dru could drop aiee_* floors unasserted; carrier P5
  proved DRUs do get hand-edited. Confirmed during apply: carrier's shipped hand DRU is
  missing 7 of 8 floors (see Applied P5-5 caveat).
- F8 (row 78): per-net width floor can be unmeetable at fine-pitch pads; mitigated only by
  constraints-authoring convention (pd-trigger /VIND 0-ohm stub).

## Applied
- P5-1 (Batch E, f48197d): rules_gen derives copper-oz from the stackup's outer copper
  (stackup resolution moved before fabfloors.profile, rules_gen.py:485-513), reports
  copper_oz in JSON, exits 2 on explicit --copper-oz vs explicit --stackup mismatch.
  Live repro fixed: 2-oz stackup now yields 2layer_2oz + VBUS 1.75mm. board-setup.md
  step 3 updated (F2 closed). DEVIATED (narrowing): mismatch refusal fires only for an
  EXPLICIT --stackup; with a defaults-resolved stackup an explicit flag still wins -
  preserves template-gen paths, matches the spec's "BOTH given" wording. 3 new tests.
- P5-3 ==P2-4 (Batch E): ONE implementation - rules_gen voltage_rules() emits named HV
  clearance rules from voltages/voltage_pairs via check_creepage's table (no duplicated
  numbers). Reconciled: names encode voltage (aiee_hv_48v_<net>, P5-3's row-117 guard) not
  P2-4's scheme; coating-aware max(track,land) rows (48V/none 0.60, 48V/mask 0.40); net
  rules ascending for later-wins; explicit pairs last incl. sub-threshold waiver rule.
  Live-proven on 10.0.3: VBUS@48V fires 24 violations named aiee_hv_48v_VBUS. 5 tests.
- P5-4 (Batch E): V12 guard - aiee_diff_outer_only_<base> "(layer inner) (constraint
  disallow track)" per solved diff pair; existing Rule class rendered it. Live-proven:
  injected In1.Cu segment fails DRC; check id = items_not_allowed, wired into
  reference/remediations/items_not_allowed.md + cluster_violations FIXER_HINTS. 2 tests.
- P5-5 (Batch E): fabfloors.DRU_FLOOR_KEYS (shared writer/checker map, anti-drift
  asserted) + check_dru() balanced-paren scan + rules_gen --check-dru mode (exit 0/1,
  cap row via P5-1 derivation). Row 160 closed L2->L3. Live: pd-trigger DRU passes rc 0;
  CAVEAT/live finding - carrier's shipped hand DRU FAILS rc 1, 7 of 8 floors missing
  (board intentionally not modified; flag when carrier resumes). Gate wiring one-liner
  left to gates owner. 5 tests.
- P5-7 (Batch E): board_init.stackup_freshness() warns (report + worker_notes, exit 0)
  when a jlc_open_api impedance stackup's verified date is >14 days old; unparseable date
  warns with age_days null instead of crashing. Kept out of bench metrics, pinned by
  test_bench_score_p5_excludes_freshness. Row 169 L1->L2. 3 tests.
- P5-2 (Batch K, ad2576c - sequenced after E so the corrected answers froze): score_p5
  rules_gen leg after clean board_init; deterministic metrics capability_class
  (2layer_2oz), netclass_split [Pwr_0p25mm, Pwr_1p75mm], power_widths_mm (VBUS 1.75),
  rule_count 12, pro_floor_failures via independent fabfloors.check_pro (weight 25).
  Baseline re-recorded same commit 100.0 -> 100.0 with new metrics block. DEVIATED
  (naming): scalar vbus_width_mm shipped as mapping power_widths_mm - fixture-agnostic,
  identical drift detection.
Batch E tests 48 -> 66, all green incl. live kicad-cli legs; 440 adjacent green. Batch K:
test_bench+test_lib_hygiene+test_board_setup 139 green.
No refuted eval claims: both batches landed the eval's diagnoses as specced; deltas above
are reconciliations/narrowings, not corrections.

## No change
- lib/fabfloors.py check_pro half - single-source verified end to end; both shipped-defect
  regression tests pass. Do not touch.
- board_init outline/cutout/corner-radius/mounting-hole handling - live-tested, honored on
  real boards (carrier 3.0mm radius, 5th hole).
- stackups.yaml provenance discipline - retired entries kept for loud by-name refusal in
  BOTH writers; phantom-3313 postmortem encoded.
- derive_copper_oz (order_submit.py:256-303) - T1 fail-loud verified, tested.
- impedance.py math - IPC-2141A microstrip correct and tested; the gap is scope (V12,
  now guarded by P5-4), not correctness.
- board_init transient-silk partition; DRU later-wins ordering; per-width netclass split
  (T1 bucketing correct - residual risk was only the P5-1 input trap, now closed).

## Deferred / rejected
- P5-6 SKILL.md inline-default: STILL PENDING - reserved for the orchestrator session per
  apply plan; SKILL.md:112-114 verified unchanged ("spawn board-setup"). Since P5-3
  landed, the proposed replacement's voltages spawn-trigger clause can drop. Spec: eval
  JSON P5-6 + batch_E skill_md_notes.
- P5-8 defer bundle (recorded, not built): (1) 4L bench fixture - path already live-tested
  by test_board_init_end_to_end; (2) row 78 pad-aware width floors - needs pad-geometry
  measurement, crosses P6/P7 ownership, /VIND workaround proven; (3) 6-layer - do not
  widen board_init choices=[2,4] without a probed 6L stackup entry; (4) epsilon_r - API
  returns no Dk, values stay epsilon_r_assumed + human calculator cross-check; (5)
  credentialed template re-probe -> P10/ordering owner. Spec lives in eval JSON P5-8.
- Ladder rows left open: 78 (pad-unmeetable floors), 117 (HV pad-escape pre-check - P5-3
  deliberately excludes pad-pair exclusions), 169 residual (live re-probe), 179 (new,
  Batch K: CRLF fixture-pin gotcha, verify_fixture refusal idea). Rows closed: 160
  (L2->L3), 169 detection half (L1->L2).

## Bench
fixture                     before   after    note
pd_trigger_board_init (P5)  100.0 -> 100.0    Batch E: delta 0, no metric diffs (fixture
                                              20V/2oz/vendor_page - no HV/freshness path)
pd_trigger_board_init (P5)  100.0 -> 100.0    Batch K: re-recorded SAME commit - rules_gen
                                              metrics block added (T5 rule: legitimate
                                              metric change re-records atomically); ran
                                              twice post-record, delta 0
Collateral sweep (Batch K): all 15 pre-existing fixtures delta 0, all P8/P9 mutants still
matched. Coverage note: F6 closed - netclass flatten, width regression, or floor drop now
moves a committed number; 4L board_init remains pytest-live-only (P5-8 item 1).

## Cost & tiers
- Now ~20k tok/run (spawned board-setup: prompt + 2-3 script JSONs + digest turns);
  target ~10k with inline default - unrealized until P5-6 lands at the orchestrator.
  Wall: board_init live leg 2.86s on 30 parts.
- board-setup, standard boards: sonnet low - "script-driven, no judgment" per its own
  prompt line 1; prefer inline (no spawn at all).
- board-setup, HV/odd-stackup/library-repair (carrier-class): sonnet medium - residual
  judgment shrank further now that P5-1/P5-3 script the two biggest loads.
