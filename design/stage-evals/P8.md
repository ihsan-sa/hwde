# P8 Verification - T6 evaluation (2026-08-06)

Two lenses: A = carrier 37.6 triage (honest partition of 124 errors), B = check suite +
fix loop + review. Applied as Batch H, commit b19d6b8 (+ cross-cut XC-2/XC-3/XC-6/XC-7).

## Verdict
- correctness A 7.5 / B 7.0: checks mechanically sound, all baselines reproduce delta 0.0;
  residue is adoption gaps (sidecars never used T2 keys) and fix-loop plumbing, not check math.
- output_quality A 6 / B 8: carrier 37.6 was 49% false-severity + 13% unfixable-by-construction;
  the verify-reviewer is the stage's star (shipped-board-saving finds 4/4 runs).
- token_cost A 6 / B 7: false-severity errors drove fixer dispatch + manual IPC re-adjudication.
- wall_time A 8 / B 7: deterministic suite 0.4-1.2s/fixture; bench wall is script, not LLM.
- knowledge_level A 7 / B 7: T4 remediation layer ahead of the sidecars, but never reached a
  production fixer (routing bug); doc layer correct, wiring dead.

## Findings
- Adoption gap: check_current plane_fed + check_creepage coating built at T2 FROM carrier
  measurements, never declared by any sidecar. 61/124 carrier errors false-severity
  (check_current.py:32-43 vs fixture constraints.json; LEARNINGS.md:1588 - 26/44 clusters
  had zero feasible via-doubling positions).
- 16/124 = same-footprint pad pairs (U1 QFN thermal 0.200mm, U22 x7, U20, C1 at 114V) that the
  P8 digest itself hand-adjudicated as vendor land pattern, not layout scope (P8-digest.md:71-73).
- Full-rail-budget attribution on routed rails with no overrides ever declared (~15/124, +12V).
- Waiver all-or-nothing: carrier verify gate FAILED 115/119, waived wholesale at H4; permanent
  gates.verify=fail on a shipped board despite human-adjudicated-justified residuals.
- fix_dispatch dead end: 19/19 production orders got fixer='review' (reviewer kinds absent from
  FIXER_HINTS, cluster_violations.py:159); remediations=[] on 23/23 workorders - the whole T4
  investment never fired in production. Carrier bypassed dispatch entirely (4/4 hand-authored).
- check_current blind to undeclared return nets: pd-trigger 5A GND choke (0.2mm necks/0.80A,
  1 via) sailed through verify 8/8, reviewer-caught only (pd-trigger P8-digest.md:2-5).
- gate.py: --commit was repo-root `git add -A` (ladder row 59); drc on stale fills graded ~375
  phantom errors, no freshness preflight (row 128, LEARNINGS.md:1445).
- 4/8 checks never produced a true finding (diffpair/thermal/silk/pdn); check_silk measured
  over-pad/thin while the real defect class was refdes ATTRIBUTION (carrier: 95 refs off by
  median 4.079mm, 111 move_text ops). Sim leg (~1500 lines) zero production executions.
- Real safety defects (equal-potential 57V tap pair, netless board-lock pads, DRU holes) found
  only by hand; check_hv_coverage (ladder row 147) still open.
- constraints twin drift: carrier architecture/ vs kicad/ copies diverged 8733 vs 14985 B,
  61-vs-53 undersized_track depending on copy (row 154).

## Applied
All in b19d6b8; tests green offline (one pre-existing live-network parts failure, Batch C scope).
- P8A-1 sidecar calibration: frozen carrier constraints.json gains plane_fed:true (+3V3) and
  coating:soldermask, sha re-pinned, baseline re-recorded same commit. Alone 37.6->62.1
  (measured pre-landing on scratch copy, zero new findings). Live workspace
  boards/lumina-carrier/kicad/constraints.json NOT mirrored - board-workspace scope, future
  live run disagrees with fixture until owner mirrors (batch caveat 4).
- P8A-2 land_pattern_pitch waiver class: check_creepage same-footprint pad pairs -> warning
  with waiver_class + same_footprint extras + summary count; follows return_path precedent
  (auto-derived from ground truth, visible, never dropped). creepage.md updated.
- P8A-3 carrier known_answer: 8 signatures pin the surviving true-defect clusters
  (undersized_track V48_RAW/+48V_SW/+12V, transition_vias +12V/V48_RTN, pour_neckdown +3V3,
  creepage V48_RTN, corridor_void /ETH_RXN); matched=8, asserted in test_bench. Future check
  weakening now trips known_answer_missed at weight 40.
- P8A-4 adoption lint, facts-only: coating_undeclared_hint (creepage) and plane_fed_candidate
  (current) - zero severity changes, closes the L0->L2 gap that cost 2 fixer attempts + 1
  retracted edit on carrier.
- P8B-1 domain routing: verify-reviewer.md findings schema gains required domain field (held
  at 50 lines); cluster_violations routes on unanimous valid domain before FIXER_HINTS
  fallback; sim kinds added. Ends the 100% dead-end routing; T4 refs can now reach fixers.
- P8B-2 (==XC-3): gate --commit scoped to boards/<ws> derived from input path, outside-dirty
  reported as excluded_dirty, never swept; stale-fill preflight exits 2 with refill message.
  DEVIATED: keyed on new drc_options.require_fresh_fills instead of parity, because XC-6
  turns parity on for the P6 gate whose boards legitimately predate fills. Rows 59+128 closed.
- P8B-3 DEVIATED: derived return-net coverage in check_current landed, but (1) synthesis gated
  at max declared rail >= 3.0A (unconditional fires a 0.07mm sliver on rf4 golden at 0.15A),
  (2) ALL derived findings advisory warnings, not errors - the shipped clean pd-trigger board
  itself fires 2 pour necks at the heuristic 5A budget, falsifying the eval's "stays
  error-clean" claim (correction recorded). mutant_gndchoke_verify NEW: pre-fix P8 snapshot
  frozen, baseline 94.8, ka matched=2. Weak discriminator (fixed board fires same kinds at
  better numbers); pins class VISIBILITY, which is what the plan's fallback allowed.
- P8B-4 verify-gate waiver sidecar (reports/verify-waivers.json): reason+approved mandatory
  else exit 2, exact check/kind + net + refs-subset matching, waived echoed in result.
  Carrier-class boards can now record a true gate pass; fixtures carry no waiver files.
- P8B-5 DEVIATED: silk_misattributed stricter than spec - >1.0mm beyond own pad extent AND
  another ref's pads nearer and <1.0mm (not offset-only), calibrated to reproduce carrier's
  exact 3 shipped residuals with zero noise elsewhere. Found a TRUE instance in the rf4
  GOLDEN (C14, 4.14mm own / 0.50mm to C1) - pinned exactly in 3 corpus tests rather than
  editing the generator-owned golden; planesplit mutant inherits it (99.5->99.4, ka intact).
- P8B-6 constraints_drift warning in verify_all when the architecture/ twin parses different
  (formatting drift excluded, no winner picked). Subsumes P2-5. Row 154 closed at L2.
- P8B-7 remediation refs pour_neckdown/decoupler_loop/gnd_stub_long (T4 format, real LEARNINGS
  cites); TOP_FIRING extended +3 with honest update-comment (the old static list could never
  detect a gap).
- XC-2 fix_dispatch merge_small_clusters: same-fixer clusters <=2 violations batch to one
  order (cap 8, union refs/kinds/region). XC-6 P6 drc gate parity:true. XC-7 test_report
  git-status diff scoped to workspace (verified live with dirty repo).
- Correction (cross-stage, recorded here because Batch H adjudicated it): P7B-6 REFUTED -
  already implemented at T2 (46900a0, matched_terminals same-ref no-cap); skipped, verified
  not duplicated.

## No change
- The 47 surviving carrier errors are TRUE board defects and stay - owned by a later
  lumina-carrier fix run, now pinned by known_answer. Includes +3V3 In2 pour-trunk necks
  (correctly stay error under plane_fed: "a trunk neck is real").
- verify_all orchestration, checklib core/exit contracts, cluster (net,kind)+spatial-split
  mechanics, gates.yaml thresholds, fixer.md protocol, bench P8 scorer/weights (0.5/0.1
  chosen for gradient) - all verified correct, keep.
- verify-reviewer.md beyond the one-word domain field: highest-value 50 lines in the stage.
- check_return_path post-T2: B.Cu-by-construction class gone, auto-waiver design correct,
  planesplit mutant proves detection intact. check_current bridge=false severity-not-reduced
  decision documented and correct.
- reference/remediations/creepage.md was AHEAD of the sidecars, not behind (it already
  encoded both P8A calibrations); older refs (undersized_track.md) carry stale line numbers +
  one pre-T2 claim - noted for orchestrator, out of batch scope.

## Deferred / rejected
- P8A-5 check_hv_coverage (DRU coverage audit, |V|>=30): deferred - needs verify_all --dru
  interface + fixture dru sidecar in one coordinated change. Ladder row 147 left open, no
  false closure. Spec: P8 eval lens A JSON (durable record), LEARNINGS.md:1785-1806 algorithm.
- P8A-6 free-form constraints waivers[]: REJECTED, position recorded in
  design/routing-knowledge-notes.md - both shipped waiver classes are auto-derived from
  ground truth; a match-anything list is ambient check-weakening. Revisit only on a third
  class ground truth cannot derive.
- P8B-8 sim leg activation: deferred to T7/P2 owners - zero production executions ever;
  trigger belongs as a deterministic rule (current_a >= 3.0 or analog block -> spawn). Spec
  in P8 eval lens B JSON + batch H report.

## Bench
before -> after (baselines re-recorded in b19d6b8 where noted):
  carrier_verify            37.6  -> 68.2   re-record: calibration by construction; 47 err /
                                            83 warn == eval's hand partition, ka matched=8
  pd_trigger_verify         100.0 -> 94.4   re-record: 56 advisory warnings legitimately fire
                                            (53 derived GND + 3 real silk); 0 errors,
                                            forbid_errors holds
  mutant_planesplit_verify  99.5  -> 99.4   re-record: rf4 golden's TRUE C14 silk finding
                                            inherited; corridor_void ka intact
  mutant_returnvia_verify   99.5  -> 99.5   delta 0.0, ka fires (rail 0.5A < 3A threshold)
  mutant_gndchoke_verify    NEW   -> 94.8   P8B-3 fixture, ka matched=2 (pour_neckdown +
                                            transition_vias on GND)
No-collateral: mutant_cpl_dfm 99.4, pd_trigger_dfm 98.0, pd_trigger_arch 91.0, P6 x2, P10
all delta 0.0.

## Cost & tiers
- Carrier-class run (lens A): ~350k -> ~250k tokens. False-severity errors drove 1-2 extra
  fixer spawns + the manual IPC re-adjudication pass; both now pre-triaged warnings.
- Median run (lens B): ~180k (range 80k usb-buck to 400k+ carrier) -> ~140k. Drivers: domain
  routing ends wrong-toolset thrash, return-current + silk-attribution move to 0-token
  deterministic checks, XC-2 batches same-domain singletons.
- Tiers: verify-reviewer fable high (max only on novel/HV first-of-class) - the only net for
  check-blind classes, do not economize. Fixer copper/router/plane opus high (escalate fable
  for HV geometric optimization); silk/schematic/parts/fab/library sonnet medium
  (schema-bound ops, deterministic gate). sim-analyst opus high - revisit after first real
  production spawn, do not spend fable on an unexercised leg.
