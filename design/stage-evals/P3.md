# P3 Parts+Library - T6 evaluation (2026-08-06)

Sources: eval lens A (script toolchain), eval lens B (agent roles + cost); applied via
T6 Batch C (216c66c, 10 items) and Batch K (ad2576c, P3-BENCH). Batch reports override
eval intentions below.

## Verdict
Lens A (scripts): correctness 6.5 (lib_pull success gate inverted BOTH ways, live repro),
output 8.5 (24/24 clean pd-trigger pull; fpfix 16->0 measured), tokens 6.5, wall 6,
knowledge 5.5 (13 verified-open ladder rows at L0 - largest open concentration in repo).
Lens B (roles/cost): correctness 7.5 (grounding absolutism held vs 3 wrong orchestrator
premises), output 9 (caught SCT2A25 FB-ref + CH224K VBUS abs-max board-killers pre-P4),
tokens 5 (97KB groundings; carrier P3 died at API spend cap), wall 6.5, knowledge 6.

## Findings
- lib_pull per-part gate judged success by grepping the .kicad_mod for the LCSC id: shared
  footprint -> false "error", 403'd symbol -> false "pulled", C2580 substring-matches
  C25804. The T1 fix walked into LEARNINGS L1134's documented trap. Fires on every
  multi-part pull with shared packages (lumina-par: 5 parts on C0603, 12 on R0603).
- Symbol lib not idempotent: easyeda2kicad dedup checks raw name, writes sanitized name;
  space/slash names re-append every re-pull (18 -> 21 symbols). L587, row 64.
- www.lcsc.com datasheet URLs unfetchable (JS shell, curl exit 0); wmsc rewrite lived only
  in LEARNINGS prose (7/9 strobe, 15/18 par re-derived by hand); datasheet_extract had no
  %PDF guard, so HTML could ground the ONLY pinout source P4 wires from. L999/L1035.
- parts_search: "10K" returns zero rows, indistinguishable from stock-out (L1026); no
  pacing in lib_pull vs measured CloudFront WAF budget (blocks after ~13 parts, L1153);
  fp_verify modal pad size let an SSOP-20 tie hide a 0.25mm-over column (L1200); no drill
  parsing so THT annulus unchecked; pulled pin types junk, retype script per-board (L531).
- Roles lens: groundings ship every page (C32843 96.8KB vs 3-24KB extractions);
  layout_notes extracted but had zero downstream consumers; librarian burned 12-24
  sequential pull turns babysitting the WAF; sourcer spent judgment turns on rule-
  determined E24 passives; pd-trigger fully extracted C14170 then the P2 amendment
  removed the part.

## Applied
All Batch C, commit 216c66c; tests test_parts.py 33 -> 54, full hermetic suite 1025 pass,
net smokes green (real C1525 pull passes new gate + kicad load).
- P3-PULL-GATE: success = symbol block with (property "LCSC Part" <id>) whose Footprint
  resolves in aiee.pretty (fplib.symbol_index); footprint-grep gate removed, exact-id
  match kills the substring trap; _dedupe_symbols (keep-first, fpfix.top_level_nodes)
  after every batch. lib_pull.py, lib/fplib.py. DEVIATED (minor): fp_reports lists the
  one footprint the symbol names, not every grep hit - per spec intent.
- P3B-1 + P3-PULL-PACE (merged): --parts parts.json batch mode (one of --lcsc/--parts),
  --pace-s auto (0 <=10 parts, else 15s per L1153 budget), 90s backoff + single retry on
  403 with on-disk re-verify; librarian.md step 1 -> one batch invocation. DEVIATED:
  per-part report extends existing results[] rows, not a parallel parts[] list.
- P3-WMSC-PDF == P3B-3 (implemented once): partslib.fix_datasheet_url rewrites www.lcsc
  form to wmsc mirror inside normalize() (covers live + db rows - not duplicated in
  _db_row_to_item, redundant); datasheet_extract --pdf exits 2 on non-%PDF bytes naming
  cause + wmsc remediation. Closes the HTML-as-pinout hazard at the deterministic layer.
- P3B-2 grounding trim: pages keyword-scored vs 12 schema-need terms; non-matching pages
  become {page, first_line, chars} stubs; --full-text escape hatch. Over-inclusive
  matching errs toward keeping pages. Schema/template untouched.
- P3-QUERY-NORM: empty live result retries once with value tokens spelled out (10K ->
  "10 kohm", 4R7 -> "4.7 ohm"), query_retried reported; every raw-empty payload carries
  the stock-out-vs-bad-query hint. Live path only (--db LIKE lacks the quirk).
- P3B-4: parts_search --pick basic-passive --qty N (Basic, 0402/0603/0805, stock >= 5x,
  cheapest wins; distinct no_candidate). DEVIATED: no_candidate exits 0 (SPEC: a search
  has no exit-1 notion). part-sourcer.md rule 1 delegates the pick; 52 lines (was 53).
- P3-FPV-GEOM: pad_size bounds MIN and MAX distinct sizes (tie can no longer hide);
  fplib parses (drill) into Pad.drill; new error-severity THT checks annulus_floor
  (<0.15mm JLC floor), drill/annulus mismatch vs new OPTIONAL schema fields; NPTH
  excluded. Warning severity for pad_size kept BY DESIGN (row 111 target revised L2->L1).
- P3-PIN-RETYPE: new scripts/lib_pin_types.py productionizes the stm32-blinky recipe
  (datasheet-JSON-keyed retype via LCSC Part property; unmatched symbols blanket-passive;
  idempotent). DEVIATED from row 60's literal "port into lib_pull": retype needs extracts
  that do not exist at pull time - standalone script per the eval's own reasoning.
  librarian.md gains step 2, held at 44 lines.
- T6-P1-3 placeholder filter (cross-stage, landed here): parts_search default-excludes
  rows with placeholder brand AND empty datasheet; --include-placeholders restores;
  part-sourcer.md:44-46 trimmed. Scout inherits via the script.
- P3B-6: placement.md Inputs names parts/<lcsc>.json layout_notes - the field's first
  downstream consumer; 63 lines held.
- P3-BENCH (Batch K, ad2576c): new bench stage P3 (live:required) - fpfix vs real scratch
  DRC on the frozen pristine set. DEVIATED: known answer pinned via fixture args +
  P3-specific penalty weights (before_drift w40, after_violations w10, residue w10,
  missing_footprints w25), not the manifest known_answer mechanism (findings-signature
  gated); same guarantee strength. Wall ~5s vs eval's 1-2min estimate.

## No change
- fpfix rules A-D and lib_refdes_norm: measured 16->0, idempotent, done-rows verified.
- fp_scratch SWIG worker; partslib ranking/filters/db fallback; parts_search exit codes;
  datasheet_extract schema + --validate: spec-compliant, keep.
- Per-IC parallel extractor shape: isolation is what let carrier extractors override 3
  wrong premises; do NOT batch - cost lever is payload size (P3B-2), not spawn count.
- part-sourcer as distinct role: judgment parts_search cannot do (stock-risk sibling
  swap, placeholder rejection, 3-round magjack qualification). Librarian blocking
  position pre-P4: kept - made carrier's creepage pad fix cheap.
- fp_verify severity split: all 7 warnings across runs benign, both real traps caught.
- Row 104 (D2PAK pad renumber): T3 DECLINE correct, settled - owner is extraction/P4.
- Row 8 (per-part DRC baseline): stale as written, superseded by scratch_drc; fold any
  residue into P3-BENCH later.

## Deferred / rejected
Eval JSONs are the durable spec for each.
- P3-DS-CROP (row 84): --crop/--zoom single-page extractor; convenience only - both S14
  extractors applied the L851 recipe from prose. Priority 3, not built.
- P3B-5: cross-run extract reuse wording in SKILL.md P3 bullet; accepted in spirit,
  left for the SKILL surgery session (no SKILL.md items in Batch C). OPEN.
- P3B-7: hold extractor spawns for parts under open P2 bounce-back; record-only,
  1-in-4-runs occurrence; revisit on second wasted extraction.
- P3B-8 (row 102): part_errata.yaml for adversarial pin tables; two known errata, zero
  shipped defects - becomes priority 1 the first time a transposed table survives.
- Row 65 residual: in-script --url fetch mode deliberately not built (the wmsc rewrite
  makes the agent's own fetch work); row stays open at L3.
- Rows 124 (3D origin offsets) and 112 (RthJA - owner is check_thermal.py, not P3):
  defer without action.
Ladder: rows 60/64/99/100/101/108/109/115 -> done; 111 done (target revised L2->L1);
65 open residual; new row 179 (Batch K CRLF fixture-pin gotcha, LEARNINGS 2269).

## Bench
Stage had NO bench before T6 (bench.py STAGES covered P2, P4-P10 only).
  pristine_lib (P3, NEW)   before: n/a   after: 100.0
Known answer: before by_check exactly {annular_width:2, clearance:2, padstack:2,
silk_overlap:8, silk_over_copper:2} (total 16) -> 0 after fix_lib + refdes_norm; ran
twice, delta 0. Baseline recorded same commit (Batch K). Batch C itself bench-neutral:
verified by grep that no scorer imports any touched module; all 15 pre-existing fixtures
delta 0 in Batch K's collateral sweep, all mutants still matched.

## Cost & tiers
Tokens/run (estimates, no per-agent telemetry): lens A ~180k -> ~155k (sourcer dead
round-trips + librarian re-pull churn removed). Lens B by class: blinky ~130k -> ~90k,
4L ~250k -> ~170k, carrier ~650k+ -> ~400k (grounding trim -50-70% on large ICs, batch
pull -40-60% librarian turns, passive auto-pick -30% sourcer turns, reuse -1 extractor
per recurring IC). Extraction visual cost is inherent.
Tiers (lenses diverge; both recorded):
- part-sourcer: A says opus/high floor (false-stock-out re-architecting is the expensive
  failure); B says sonnet/high, escalate to opus/fable for novel-domain screens only
  (carrier magjack class). Agreement: never below sonnet/high; escalate on novel
  interfaces.
- datasheet-extractor: sonnet both; A high effort (vendor table traps L1045/L1054),
  B medium floor. NOT haiku - plausible-wrong pin passes schema validation. Reserve
  opus for first-seen high-pin-count parts.
- librarian: sonnet both; A medium (post-gate-fix errand), B high (SVG overlay reading,
  voltage-derived pad surgery). Post-Batch-C the churn half shrank: medium for pulls,
  high when edits touch geometry.
