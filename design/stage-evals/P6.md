# P6 Placement - T6 evaluation (2026-08-06)

Lenses: A (algorithms + bench), B (run evidence + template mandate). Applied batch F, commit eb8c181.
Batch report overrides eval intent below; eval JSONs in scratchpad are the durable proposal record.

## Verdict

Scores lens A / lens B (0-10):
- correctness 7 / 6.5 - deterministic core (seed, SA, atomic place_edit) sound and tested; but
  placelib dropped per-pad rotation (2 boards measured wrong) and place_edit shorted a routed board.
- output_quality 8 / 7.5 - 4/4 boards first-attempt place-gate pass, 0.97-1.0 route probes; silk
  debt on all 4, carrier shipped median refdes offset 4.079mm.
- token_cost 7 / 4 - one spawn per run but heavy tail: 30-160 hand move_text ops, pd-trigger full
  hand floorplan, WRL orientation proofs re-derived each run.
- wall_time 8 / 5 - scripts fast (carrier 116 fp anneal 346s); agent sessions 45-97 min.
- knowledge_level 5 / 4.5 - proven silk solver recipe sat as LEARNINGS prose; refuted candidate
  rule still in placement.md; corridor constraint inexpressible.
Headline: pd_trigger 18.53 composite was mostly measurement artifact (bench scored GND flight lines
and a bulk cap the annealer is correct to ignore), not placement weakness.

## Findings

- Bench/objective misalignment: bench P6 crossings/congestion counted GND flight lines that
  place_anneal excludes (MST gnd weight 0.0, place_anneal.py:79); measured blinky2 15/17 crossings
  GND, pd_trigger 15/37, congestion_max 9->5 / 7->5 excl GND. decap_worst took max over class=bulk
  caps (pd_trigger C1A 15.97mm VBUS entry cap = whole 7.99-pt penalty; true decoupler C5 3.0mm).
  ~28 of pd_trigger's 81.5 penalty pts pointed at quantities no placement change should chase.
- Corridor blindness: anneal's only current term is current_a x HPWL (place_anneal.py:444-446); on
  the 5A board every candidate violated the corridor, agent hand-floorplanned 30 parts at +40%
  HPWL (264->369.6) (boards/pd-trigger/log/P6-digest.md).
- Silk unowned: courtyard-only packing silk-blind; 30-160 hand move_text ops per board (usb-buck
  55, pd-trigger 30, carrier 160 across 9 files + P8 workorder); solver recipe was prose only
  (LEARNINGS:687-730, ladder rows 71/53).
- placelib FpPad dropped per-pad rotation: pad boxes wrong at 90/270 (ROT-90 SOT-23 under-reports
  0.275mm/side); S14 effective-courtyard union built from wrong extents; usb-buck agent re-derived
  J1's 7 pads by hand (LEARNINGS:667, 1167; ladder rows 69/110).
- placement.md:35-38 stated the refuted "prefer the seed" rule - S14 evidence: repairing best
  candidate beat seed on every metric (HPWL 227 vs 330, crystal 4.1 vs 12.0mm) (LEARNINGS:576b).
- place_edit applied footprint moves on routed boards with no guard - one board shorted while both
  P6 oracles stayed green (LEARNINGS:1750, ladder row 130).
- _repair cannot evict a blocker (blinky C1 stuck 8.7mm behind D2; agent built 2-level cooperative
  search in-run to reach 2.7mm) (LEARNINGS:576a, row 63).
- Connector mating direction seed-guessed, re-proven from WRL each run (pd-trigger J2 backwards,
  usb-buck J1; rows 70/74). Hand-listed group membership unaudited (carrier eth_xtal omitted
  R35/R36 -> 115.6mm oscillator copper, P6/P7 backward edge, -71% after fix).

## Applied

All in eb8c181; full offline suite 977 passed / 0 failed, smoke 9+1 passed.
- P6A-1 bench signal metrics: placelib.class_sets single net-class source (place_anneal imports);
  crossings/congestion gained exclude=; place_metrics emits crossings_signal +
  congestion_signal_max (all-net kept); bench score_p6 switches to signal variants under unchanged
  penalty keys; decap_worst exempts only EXPLICIT class=bulk sidecar caps (unclassed caps still
  count - p8's C5 caveat honored). Files: placelib.py, place_metrics.py, bench.py,
  place_anneal.py, both baselines. Both baselines re-recorded same commit; --compare delta 0.0.
- P6A-2/p3 per-pad rotation, DEVIATED: FpPad.rot stores RELATIVE rotation captured at parse
  ((file ROT - fp angle) % 360), not the spec's absolute-with-(p.rot - self.angle)-at-box-time -
  spec's version corrupts boxes after in-memory place_center rotation (annealer). Cumulative file
  convention machine-verified on live boards. ROT-90 SOT-23 0.7x1.25 now yields 1.25mm-wide box.
  Legality stayed 0 on both fixtures; pd_trigger utilization 0.330->0.329 (recorded in baseline).
  3 new rotated-pad tests. SKILL.md line untouched (reserved for orchestrator surgery).
- p2/P6A-3 placement.md rewrite (line-count neutral, 63 lines): refuted seed rule replaced with
  repair-the-BEST-candidate + S14 citation; side-render / WRL pin-fit orientation proof now
  required (row 74 L0->L1); stage-3 silk flow routed through silk_place.py; stage-2 notes
  --margin-mm and placement.corridors.
- P6A-4 routed-board guard: place_edit refuses footprint ops on boards with (segment/(via unless
  --allow-routed (regex avoids "(vias not_allowed)" false positive); text/lock ops exempt; flagged
  runs report routed_board:true + re-run-DRC warning. Guard in run() only, so importing callers
  (seed --apply, anneal, route probe) unaffected - deliberate. 4 new tests. Row 130 done L2.
- P6A-6 --margin-mm (default 0.0 = byte-identical, proven by determinism tests + bench delta 0):
  buffers body/obstacle polys by margin/2 for SA overlap + _repair/_spot_legal only; placelib
  legality keeps true courtyards; edge overhang exempt via out_base. 2 new tests. Row 53 done L3.
- P6A-5 corridor cost term (M version): placement.corridors [{a,b,width_mm,net?}] -> oriented
  swath; non-endpoint bodies pay CORRIDOR_W(30)/mm^2 via rule term; incremental per-(corridor,
  body) ledger + endpoint resync, full_sync drift kill; corridor_unknown_refs surfaced;
  per-candidate corridors report. DEVIATED (extension): endpoints may be FIXED/locked footprints,
  not just clusters - spec's cid-only resolution silently dropped the natural connector anchors.
  3 new tests. Seed/legality integration deferred (see p5).
- p4 crystal-island template: new placetemplates.py (252 lines) + groups[].template plumbing +
  layout_satellites hook + warn-only audit_groups from seed(). Deterministic role inference; caps
  mirrored about pad axis, GND outboard; inference failure warns + falls back generic. Membership
  audit names omitted 2-pad osc-net parts (carrier R35/R36 class). Test DEVIATION: synthetic
  carrier-equivalent, not the carrier's pre-correction netlist (not a frozen fixture). 8 tests.
- p1 silk_place.py (545 lines), DEVIATED on three points: no auto batch-bisect on DRC regression
  (agent bisects from findings); text advance uses MEASURED 0.845*size (LEARNINGS:1771) not the
  proposal's 0.75; no silk class in placelib.legality_violations (row 145 residual). Otherwise
  full recipe: both-angle four-side candidates, lib_refdes_norm default target first, crowded-first
  ordering, live .kicad_pro clearance, place_edit-validated move_text ops, --apply atomic,
  --verify-drc real-DRC oracle, honest residuals (exit 1). placelib.text_box owns metrics
  (absolute-angle contract). Smoke: blinky2 golden -> solve -> apply -> DRC 0 silk findings.
  Rows 71/145 notes updated.

Corrections (eval claims refuted during apply): P6A-2's box-time angle convention wrong as
specced (above); p1's 0.75 text-advance figure superseded by measured 0.845; p8 subsumed into
P6A-1 with its own unclassed-cap caveat confirmed and enforced.

## No change

- Decap loop template class: already exists as CODE at L3 (build_clusters + layout_satellites +
  S14 courtyard union); shipped distances in class; residual blinky C1 bleed is a SEARCH failure
  owned by deferred p7, not a missing template.
- place_edit atomic pipeline (validate -> scratch -> SWIG -> re-parse verify -> os.replace) and
  place_swig op set: correct, rollback-proven.
- Anneal engine mechanics: incremental terms + full_sync drift kill, adaptive schedule, cold-regime
  stall counting, single-RNG determinism; carrier 116 fp / 53 clusters in 346s.
- place_seed satellite slotting + spring embed (rows 44/45 done). P7->P6 backward edge protocol:
  worked as designed both firings.
- Bench harness: both fixtures delta 0.0, no env drift. HPWL penalty stays raw all-net: GND share
  modest (~18-25%), class-weighting rejected as metric churn.
- <=8 edit-iteration budget kept at 8: overruns were silk-driven; silk_place makes it realistic.
- sep_unknown_refs ERROR escalation: owned by netlist_audit (row 86), outside stage.

## Deferred / rejected

Specs live in the two P6 eval JSONs (scratchpad/evals/); ladder is the open-work index.
- p5 full corridor seed/legality primitive - cost term shipped instead; seed ALONG_AXIS ordering +
  P2 emission of the key deferred (needs P2-side work).
- p6 buck hot-loop template - 2/2 buck boards hand-placed; deterministic role inference specced;
  deferred as L-size.
- p7/P6A-7 blocker eviction in _repair - observed once, agent workaround exists, corridor term
  removes the common blocker source first; ladder row 63 script half stays OPEN (note updated).
- P6A-8 connector mouth-vector table - needs P3 pull-time hook (cross-stage); row 70 stays OPEN.
- p8 - not deferred: superseded by P6A-1 (implemented there).
- Rejected on record: full silk model inside SA (row 53) - move_text sweep + margin flag is the
  right L3 step; SA silk term not worth it.

## Bench

fixture               before   after   rationale
pd_trigger_place      18.53    48.02   P6A-1 re-baseline: crossings 37->22 signal, congestion 9->5,
                                       decap 15.97->3.0 (explicit bulk exempt). Eval predicted ~48.0.
golden_blinky2_place  53.36    72.36   crossings 17->2 signal, congestion 7->5, decap unchanged
                                       7.06 (no explicit bulk in sidecar). Eval predicted ~72.4.
Re-baseline rationale: CHECK defect, not board change - penalties measured quantities the annealer
correctly ignores; signal terms keep nonzero gradient (22 / 2 crossings). Both re-recorded in
eb8c181 with the bench.py edit; --compare delta 0.0; P8/P9/P10 mutant fixtures untouched (green in
the 977). Note bench scores FROZEN artifacts: seed/anneal/template changes are structurally
bench-neutral; proven by unit fixtures, optionally by --artifact runs.

## Cost & tiers

Tokens/run: ~100-150k now -> ~80-120k est. (lens A); lens B: ~150k -> ~90k after silk loop
(1/3-1/2 of edit iterations) and orientation proofs disappear and corridor removes hand-floorplan
risk. Landed this batch: silk_place (p1), orientation-proof mandate (p2), rotation fix (p3),
corridor cost term; buck template still open, so buck boards keep a hand-place tail.
Model/effort: placement agent = fable/high (highest-judgment role: candidate selection, recovery
when the optimizer fails; even strong models made geometry errors here - carrier C35 off by exactly
0.7mm introduced a short). Scoped P7 backward-edge re-spawn = sonnet/medium (bounded,
script-verifiable). Sonnet/high trialable for 2L blinky-class boards only after templates bed in.
Deterministic P6 scripts (seed/anneal/metrics/edit/silk): zero tokens, never spawn an LLM.
