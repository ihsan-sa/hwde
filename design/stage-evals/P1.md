# P1 Research - T6 evaluation (2026-08-06)

Eval: 4 agent roles, whole stage, no scripts, no bench stage; cost-primary lens.
Applied via Batch B (commit 1746285) + one item pre-landed in Batch C (216c66c).
Durable specs: scratchpad eval JSON T6___P1_Research_whole_stage (proposals verbatim).

## Verdict

- correctness 8 - fragments proved right downstream: both shipped boards passed verify 8/8
  first try; carrier P1 disqualified the brief's named PD part with standards citations.
- output_quality 9 - par P1 caught a 10x requirements error; pd-trigger interface agent found
  the diff_pairs:[] DP/DM-short trap and the 20V-rating trap.
- token_cost 5 - pipeline's biggest token fan: unbounded artifacts (44KB-567KB/run) read
  wholesale by P2 architect and P3 sourcer.
- wall_time 7 - fan-out parallelism works; roster self-collapses on small boards (0 spawns
  on stm32-blinky).
- knowledge_level 4 - standard interface facts re-derived at opus cost every run; design-notes
  sec-6 reference dirs (topologies/, interfaces) did not exist; recurring knowledge sat at L0.

## Findings

1. Unbounded scout dumps: full parts_search sweeps (45 result rows, 6-tier price breaks) in
   research/*.json despite the prompt's own "Shortlist 3-6"; strobe protection-sense.json 91KB;
   run totals 567KB (carrier) / 505KB (strobe) vs 44KB shipped usb-buck. 3/3 lumina runs.
2. Standards facts re-derived per run: USB-FS, USB-C PD sink, Ethernet MDI each fully
   re-researched; routing-knowledge-notes.md:262-266 names references/topologies/ + interface
   fragments as targets - neither dir existed. 3/6 runs.
3. Scout JSON shape drift: no schema, each run invents one (pd-trigger {candidates:[...]} vs
   strobe {results:[...]}, different price keys); P3 tolerates only because sourcer is an LLM.
4. No machine validation of fragments vs constraints_schema; interface-spec prompt's own
   warning "keys you emit wrong are keys the pipeline silently ignores" was the only guard;
   carrier's "JSON-validated" was manual orchestrator work. Latent every run.
5. JLC placeholder-row hazard (found BY a P1 scout, pd-trigger P1-digest:4) encoded only as
   prose in part-sourcer.md:44-46; parts_search.py had no filter; scout side never warned.
6. Orchestrator recorded a P1 "Power topology" decision before the scout reported, without
   reading its file (carrier P1-digest:64-69); part numbers matched by luck; needed CORRECTION.
7. Repo-root pollution: research agent dropped ?? docs_start.html at root, breaking the two
   git-status-diff report tests (LEARNINGS.md:922). 1 instance.
8. Cross-stage: P1 emits `voltages` correctly but rules_gen.py never read the key - HV
   clearance knowledge dormant until P8 creepage. Recorded as evidence for the P5/P7 eval
   (landed there as Batch E HV-clearance DRU work).

## Applied

- T6-P1-1 slim scout contract (Batch B). Write bullet now mandates EXACTLY
  {block, candidates[<=6 x {mpn,lcsc,package,basic,stock,price,datasheet,rank,fit}]}; full
  sweeps go to research/raw/<block>-sweep.json via parts_search --out (script-written, never
  typed); P2/P3 read only the slim file. Fixes findings 1+3 in one shape. Prompt 50 lines.
  Files: agents/research-component-scout.md. Proof: constraints_lint enforces max-6 as a
  warning (test-covered); tests/test_orchestrator.py green.
- T6-P1-2 reference/interfaces/ seeded (Batch B). usb-fs.{json,md} from usb-buck,
  usbc-pd-sink.{json,md} from pd-trigger (diff_pairs:[] trap kept as headline note),
  ethernet-10-100.{json,md} from carrier (MDI 100R, t_rise 3.0ns, skew 2.5mm). Interface-spec
  prompt now STARTs from a matching fragment: re-verify, adapt, mark deltas - not re-derive.
  DEVIATED (sound): seeds are generalized distillations, not verbatim copies - part-specific
  halves stay in boards/*/research via _provenance pointers; stackup-dependent gap_mm dropped
  with recompute/auto-derive notes; usbc-pd-sink keeps 20V/5A worked values with explicit
  RECOMPUTE-per-profile notes. Every number keeps its source. All 3 seeds lint totally clean
  (zero errors AND warnings, test-enforced). Prompt held at 47 lines.
- T6-P1-4 scripts/constraints_lint.py (Batch B, 353 lines + 58 tests). SPEC-6 validator for
  research fragments + merged constraints.json: 9 schema-typed sections incl. T2 keys,
  required-key/type/enum errors, _-prefix allowance, envelope aliases. DEVIATED (severity
  reconciliation): eval asked WARN-level unknown keys AND exit-1 misspellings - landed as
  close-match (difflib >=0.8) unknown keys = ERROR (max_skew -> max_skew_mm typo class),
  distant unknowns = advisory WARNING; cutoff validated against all 34 shipped board JSONs
  (zero false errors). Schema-md key-set test-pinned to the script. Interface-spec "silently
  ignores" prose deleted, replaced by lint pointer. SKILL.md wiring deliberately NOT made
  (orchestrator-owned; spec preserved in batch_B skill_md_notes).
- T6-P1-5 size + scratch discipline (Batch B). Shared 2-line rule in all four research
  prompts: workspace-only writes (kills the docs_start.html class), md <= ~300 lines,
  findings + citations, never transcript/search dumps. Per-file targets met (scout 50,
  interface-spec 47, power-architect 45, refdesign 41); caveat: 4-file total 181->183 (+2)
  for all Batch B prompt additions combined - per-file targets were the binding numbers.
- T6-P1-6 reference/topologies/buck.md (Batch B, 90 lines). Class-level decisions the three
  sync-buck runs re-derived (usb-buck AP63203, carrier SCT2A25STER, par TPS92515-class):
  selection table, inductor Isat-vs-clamp/DCR rules, hot loop, BST, fixed-vs-adjustable FB
  trap, switch-node containment, upstream bulk-limit trap. refdesign prompt reads the matching
  topology file first, researches only the part-specific delta; held at 41 lines.
- T6-P1-3 placeholder filter - LANDED IN BATCH C (216c66c), Batch B skipped as no-op.
  parts_search default-excludes rows where (brand empty or in {JLCPCB, JLCPCB Assembly}) AND
  datasheet == "" - both conditions, so real-brand/no-datasheet rows (Tokmas LM5069 class)
  survive; placeholders_filtered envelope counter; --include-placeholders escape hatch;
  part-sourcer.md:44-46 trimmed to one line. Scout inherits protection through the script.
  Verified: parts_search.py:206,239. Correction record: batch ordering meant the eval's spec
  was already implemented when Batch B ran; reconciled, not duplicated - no eval claim refuted.

Tests: tests/test_constraints_lint.py 58 new green; test_orchestrator.py 80 green;
test_parts.py green except pre-existing live flake test_net_search_returns_in_stock_hits
[AP63203] - endpoint returns 0 hits with AND without the filter (stock churn, not code).

## No change

- Roster selection (SKILL.md:93-98): adaptive spawning demonstrably works - 0 spawns on
  stm32-blinky, power-architect skipped on par with recorded rationale, 8 justified on carrier.
- research-power-architect method (per-consumer current traceability, 30% headroom, >3A/mains
  forced into OPEN): sound and followed; produced brief-breaking verified findings.
- research-reference-design core rules ("decisions never files", citation-or-opinion,
  two-source cross-check): the AN956-inherited 10x error was caught BECAUSE of provenance.
- Orchestrator read-summaries-only + 5-10 line digests: all six P1 digests conform.
- interface-spec schema-first design: usb-buck fragment survived P2 merge, P5 rules, P8
  verify 8/8 first try - existence proof.
- Downstream consumption: every P1 role has a real consumer (44 research/ references across
  18 architecture files); no orphaned artifact class.
- P1 as emergent requirements back-check (par 10x catch, carrier part disqualification):
  valuable, needs no formalization; P2 arbitration already resolves research conflicts.

## Deferred / rejected

- T6-P1-7 state.py decision --source provenance warn (source_missing flag): not in the
  accepted set, not built (grep state.py confirms). Spec lives in the eval JSON, priority 3.
- T6-P1-8 merge P1 roster for small boards: REJECTED by the eval itself - roster already
  self-collapses; single-role depth is where the quality wins came from. Decision record only.
- SKILL.md lint wiring (P1-close/P2-close constraints_lint invocations) + spawn-table
  sonnet/high downgrade for canonical-fragment interfaces: deferred to the reserved SKILL
  surgery session; exact wording preserved in batch_B.json skill_md_notes.
- Ladder rows: none touched - grep-verified design/ladder-triage.md has no rows covering P1
  prompts, reference seeds, or a constraints validator; nothing to close or promote.

## Bench

P1 has no bench stage; baselines dir has arch/sch/place/route/verify/dfm/order fixtures only.

  fixture              before   after    note
  (none - P1)          n/a      n/a      all applied items bench-neutral by construction;
                                         nearest coupled fixture pd_trigger_arch consumes
                                         committed research/ inputs, none altered.

No baselines re-recorded (batch_B baselines_rerecorded: []).

## Cost & tiers

Tokens/run: ~250-500k small roster (2-3 spawns) up to ~1.5-3M carrier-class (8 spawns), plus
~140k knock-on P2-architect input on carrier-class corpora. After Batch B: ~150-350k small /
~0.8-1.5M carrier-class - scout dumps script-written (-60-80% scout artifacts), repeat
interfaces validate-and-adapt from seeds (-50-70% per repeat spawn), size caps (-30-50% P2
research ingestion); P2 knock-on roughly halves. Basis: six P1 digests (0/2/3/8/4/6 spawns),
wc -c over boards/*/research, 3-8x transcript multiplier for research-heavy roles.

Model/effort recs (eval; spawn-table edits pending SKILL session):
- research-interface-spec: opus/high for first-encounter standards; sonnet/high once a
  canonical reference/interfaces fragment exists (job becomes validate-and-adapt).
- research-power-architect: opus/high stays default (rail-tree errors poison everything;
  boards cheap enough for sonnet mostly skip the role anyway).
- research-component-scout: sonnet/high - live-proven tier (pd-trigger scout delivered the
  winning table + placeholder hazard on sonnet); high not medium for datasheet verification.
- research-reference-design: sonnet/high; escalate to opus only for novel safety-adjacent
  blocks (mains, batteries).
