# P0 Intake - T6 evaluation (2026-08-06)

Sources: evals/P0_Intake_whole_stage.json; batch report A (commit 5520483); repo verified.

## Verdict

- correctness 7.5 - all six runs produced conforming requirements docs, but the one trivially
  checkable property (artifact at workspace root) escaped once into a shipped PDF.
- output_quality 8.5 - question quality high: strobe caught a 100W-vs-15W brief inconsistency,
  par derived a firmware-hang-safe current ceiling, pd-trigger flagged the 5A copper constraint.
- token_cost 9.5 - leanest stage: one 42-line-prompt spawn per run, highest judgment per token.
- wall_time 9 - single spawn, no fix loops.
- knowledge_level 5.5 - everything sat at L1 prose in the prompt, zero L2 script enforcement
  pre-T6; Batch A gives the stage its first two L2 rungs (req-lint, env-pin).

## Findings

- FM1 artifact path unpinned (1/6 runs): analyst wrote requirements.md under architecture/;
  report_gen.py:465 hardcodes the root path, so lumina-carrier's design-doc PDF shipped with
  "requirements.md not found" (lumina-carrier-design-doc.tex:58) while the file existed 383
  lines strong one directory down. Neither agents/requirements-analyst.md nor SKILL.md pinned
  the location.
- FM2 no unattended-run protocol for the safety-blocking rule (3/6 runs, 2 with live safety
  flags): pd-trigger answered six >3A questions "as delegate", carrier invented
  D-P0-PROVISIONAL, usb-buck auto-answered with defaults - three ad-hoc bypasses of the same
  missing rule (per-board log/P0-digest.md).
- FM3 artifacts registry unpopulated (5/6 runs): only lumina-strobe recorded 'requirements' in
  state.json; carrier reached P10 with artifacts == {}, so report_gen had no registry fallback.
- FM4 stale KiCad pin accepted (latent, 0 live hits): env.py:112-117 returned an explicit
  HWDE_KICAD_CLI pin after an existence check only; check_env passed any version >= 9.0.5
  while the pipeline is 10.0.3-pinned and formats are not forward-compatible. Ladder row 18.
- FM5 zero deterministic checking of the P0 artifact (every run): no lint of location, section
  structure, or open-question format; the FM1 escape was a 10-line path check away.
- FM6 P0-discovered pipeline facts not promoted (2/6 runs): par and strobe both independently
  re-derived that board_init.py cannot cut the RJ45 notch and that --mounting-holes needs
  --margin 10 for a 5mm inset; fix owned by P5 but P0 pays the re-derivation tokens.

## Applied (all in commit 5520483, T6 Batch A)

- req-lint: new .claude/skills/hwde/scripts/check_requirements.py (130 lines, SPEC 6
  contract) - (a) root-location check with stray detection under architecture//brief/
  (req_misplaced error naming the stray, req_missing when absent everywhere); (b) '## <n>'
  coverage of sections 1..9, wording-tolerant (req_sections); (c) section-9
  numbered-question-or-'none' check as req_oq_format WARNING (exit stays 0). Prompt edited
  net-zero (42 -> 42 lines): WORKSPACE ROOT contract + HARD-vs-soft outline marking.
  Proof: tests/test_check_requirements.py (11 new tests incl. 5 real-board greens + the
  carrier escape); CLI pd-trigger exit 0, lumina-carrier exit 1 req_misplaced.
  DEVIATED: spec check (d) (section-8 heading) subsumed by (b), not duplicated; question
  regex tolerates '**1.' bold because par/strobe use it and the lint must start green;
  warnings-keep-exit-0 deviates from checklib's any-violation norm (documented in docstring)
  so a style nit cannot loop the analyst.
- env-pin: env._validate_pin(path, var, ver) in scripts/lib/env.py rejects an explicit pin
  with kicad-cli major < 10 (EnvError with unset-or-repoint remediation); wired into BOTH
  explicit paths - HWDE_KICAD_CLI and HWDE_KICAD_ROOT (DEVIATED wider than spec: the root pin
  is the same mistake class); undeterminable-version pins rejected with a distinct message
  instead of IndexError. Auto-discovery untouched - 9.0.5 stays reachable by not pinning.
  No check_env.py edit needed: EnvError surfaces through its existing except branch.
  Proof: 3 new tests in tests/test_check_env.py; integration with a 9.0.5-echoing stub .bat;
  unpinned discovery still resolves 10.x. Ladder row 18 L0 -> L2, done (verified in diff).
- reportgen-fallback: report_gen.sec_requirements resolves via (1) state artifacts registry
  (backslash-normalized), (2) root requirements.md, (3) architecture/requirements.md; off-root
  hits warn and record the actual path. DEVIATED (refinement): warning keys off the resolved
  path, not the rung, so strobe's root-pointing registry entry stays warning-free.
  Proof: 2 new tests in tests/test_report.py; live carrier --tex-only regains the full
  Requirements body, zero not-found stubs (regenerated .tex git-restored to keep tree clean;
  carrier's residual exit 1 is two pre-existing missing artifacts, not this change).
- state-scaffold: State.init creates the ten standard subdirs idempotently; init CLI payload
  gains 'subdirs'. Proof: tests/test_orchestrator.py - all ten exist, pre-existing brief/
  content survives, CLI contract asserts the list; S13 dryrun kill/resume smoke green.
  DEVIATED: the paired SKILL.md 2-line run-start trim deferred to the orchestrator
  (do-not-touch rule); exact replacement text in batch A skill_md_notes.
- XC-4-digest-discipline (cross-cutting, landed via Batch A): State.set_phase warns (exit 0)
  when leaving P<n> with log/P<n>-digest.md missing or over DIGEST_LINE_CAP=15.
  Proof: 3+ tests in tests/test_orchestrator.py (missing warns, 12-line silent, 20-line
  warns, no-op silent, CLI JSON carries warning).

Batch battery: 190 passed 0 failed (-m "not smoke" across the six touched/neighbor test
files); smokes: dryrun kill/resume 1 passed, 2 real-board PDF regens green.

## No change

- agents/requirements-analyst.md core design (9-section schema, ask-never-guess for safety,
  ONE-batch questions, ASSUMED: markers): validated against all six outputs; stays 42 lines.
- P0 spawn economics: exactly one agent, no fix-loop machinery - no tier/batching change.
- check_env.py coverage beyond the pin gap: package matrix, KiCad probe chain, Java-25/jar/
  pdflatex/ngspice remediations all correct; ladder rows 9/10 done, row 41 correctly n/a.
- state.py data model and init-leg mechanics: atomic tmp+os.replace, budgets/history/decisions
  schema, already-exists guard - correct and tested.
- requirements.md stays prose: downstream consumers are all agents; constraints.json at P2 is
  the machine artifact; six runs show no prose-transit corruption.
- No bench stage for P0 generation is the correct steady state: output is nondeterministic
  agent prose whose value is semantic; the deterministic slice is owned by req-lint + goldens.

## Deferred / rejected

- skill-delegate (prompt_edit, prio 2): codify the unattended PROVISIONAL-delegate protocol +
  lint wiring in the SKILL.md P0 bullet (lines 90-92 -> 4 lines). NOT LANDED - SKILL.md edits
  reserved to the orchestrator; check_requirements.py exists and is ready to be cited, and
  state-scaffold's -2 SKILL lines fund its +1. Verified still pending: SKILL.md untouched
  since T1 (e679027); the P0 bullet still reads the old three lines. Spec: eval JSON
  proposals[skill-delegate] + batch A skill_md_notes (exact replacement text for both edits).
- req-sidecar-defer: machine-readable caps.json sidecar REJECTED for now - all consumers are
  LLM agents, prose flowed correctly on all six runs (pd-trigger's soft-outline nuance
  survived to P5 because it was prose), constraints.json is the machine artifact of record.
  Revisit only on an observed prose-transit corruption. Durable record: eval JSON.
- FM6 (cutout/mounting-hole fact promotion) not a P0 proposal - owned by the P5 stage eval.
- Ladder rows: 18 closed by env-pin; no other P0-mapped rows left open by this batch
  (grep-verified per batch report; ladder summary counts left stale for the orchestrator).

## Bench

No P0 bench fixture exists and none was added - bench.py STAGES starts at P2
(scripts/bench.py:71-80). Recorded as the honest steady state: benching nondeterministic
requirement prose would bench the scorer, not the agent. before -> after: n/a -> n/a.
No baselines re-recorded (batch A bench/baselines_rerecorded both empty).

## Cost & tiers

- Tokens/run now: ~8-20k single-page briefs (42-line prompt + 11-15 line brief in, 92-208
  lines out); ~50-90k lumina-class multi-brief (up to 1432 brief lines in, 383-723 out).
- After: ~unchanged (+<1k for one check_requirements.py invocation + JSON). All five applied
  items are correctness/ladder moves, not cost moves.
- requirements-analyst: fable/high - highest judgment-per-token role; a missed requirement
  costs a full re-run (no incremental board update, LEARNINGS 2026-07-30); one spawn makes
  the top tier essentially free. No sonnet mode-split for blinky-class briefs - not worth
  the spawn-table complexity for one spawn.
- P0 inline (check_env + state init + question batch): stays orchestrator-inline, no
  additional agent role.
