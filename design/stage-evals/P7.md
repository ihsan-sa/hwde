# P7 routing - T6 evaluation (2026-08-06)

Lenses: A = driver scripts (route_auto/critical/edit/cleanup, stitch_vias, planes_gen,
plane_repair, lib/routelib+route_swig+netconn); B = run evidence + agents/router.md.
Applied as Batch G, commit 11ed6e5 (16 files, +1272/-89).

## Verdict
- Correctness A 6.5 / B 8: plumbing live-proven (4/4 boards DRC 0/0, completion 1.0) but
  route_cleanup's connectivity model was provably wrong and hole floors drill-blind.
- Output quality A 8.5 / B 9: exemplary judgment pivots on record (pd-trigger premise
  falsification, carrier C35 catch, honest C1B disclosure); one sanctioned backward edge worked.
- Token cost A 6 / B 4: router re-derives geometry in-context and authors ad-hoc scripts every
  complex run (47 .py files in carrier work/p7); failure tail 400k-1M tokens.
- Wall time A 7.5 / B 6: KRT trial-and-error loops (carrier ~10 rip-set attempts) dominate.
- Knowledge A 5.5 / B 6: 5 machine-verified KRT facts and the pad-width formula sat L0 in
  LEARNINGS prose instead of script flags.

## Findings
- route_cleanup dangling pass removes CONNECTED copper: _endpoint_free tested endpoint-to-
  CENTERLINE dist <= 0.01mm, blind to copper overlap. Reproduced on frozen pd_trigger fixture:
  VBUS stubs ending 0.5mm INSIDE a 3.0mm trunk's copper called free, unconnected 0->2.
  CORRECTION: bisection (dang-only -> 2 unconnected; loop-only / corner-only -> clean) refutes
  PROGRESS.md:101's V13 diagnosis "loop-breaker union-find/fill edge". 3/3 shipped boards hit.
- stitch_vias hole floor center-point + drill-size-blind (Scene.holes bare tuples, 0.5mm
  center-to-center); geom.Pad carried no drill field. 2 live instances (usb-buck escape to P9;
  ladder row 144 in-pad case). KiCad DRC also blind to this class (LEARNINGS:819).
- route_critical missing 5 verified KRT facts: no --max-iterations scaling (200k default, not
  congestion, failed 60-70mm hauls; 4M routed first try), no Coverage-line parse (static share
  >0.7 = raise iterations, never widen rip set), no --net-clearances emission (--clearance is a
  CAP; DRU-only HV clearance silently pulled 48V nets to 0.2mm -> 480 violations), relative
  --work-dir dies under KRT's cwd (LEARNINGS:1318 live hit). Cost: carrier ~3 spawns + ~10
  manual rip-set attempts (LEARNINGS:1433/1504/1522).
- planes_gen thermal grid blind to existing vias-in-pad (U22: 15 shipped + 21 added -> 24
  hole_to_hole, LEARNINGS:1327); strict < in _rects_overlap let touching rects share priority
  (zones_intersect).
- router.md:65 "never hand-edit" contradicted the sanctioned pour fan-in recipe (hand
  connect_pads patch, LEARNINGS:791-794); pd-trigger shipped via the patch.
- pd-trigger 1.75mm VBUS command geometrically unmeetable at 0.5mm-pitch pads (max connectable
  ~1.5mm); KRT 9-shorts self-rolled-back correctly; measurement lived only in prose.
- route_probe reports counts only, so topology caps read as placement congestion (row 76).

## Applied
- P7A-1 route_cleanup V13 fix: dangling touch tests become copper-body overlap (segment
  (w1+w2)/2, via barrel, pad poly, arcs carry width); loop victims that are bridges of the full
  netconn graph vetoed; one dangling re-sweep after loop removal. Live on frozen pd-trigger
  fixture: full cleanup DRC 0/0 -> 0/0, both VBUS stubs kept; regression test pins
  dangling_removed==0 + stub uuids. router.md V13 skip guidance deliberately NOT re-promoted
  until a live board proves the fix. Files: route_cleanup.py, test_route_cleanup.py (+5 tests).
- P7A-2 --work-dir .resolve() in route_critical.run, route_auto.run, route_probe; unit test
  asserts absolute ctx path from a relative arg.
- P7A-3==P7B-5 KRT facts scripted: (a) retry ONCE at --max-iterations 4000000 /
  --max-probe-iterations 60000 on "No route found" with static share >=0.7 (or no Coverage
  line); <0.7 never blind-retries, violation carries static_share; (b) coverage_static_share
  parsed into facts; (c) build_net_clearances emits work/net_clearances.json from .kicad_pro
  netclasses + .kicad_dru per-net rules (max wins, nothing capped down; parser fails open),
  passed on every KRT call incl. route_diff. Flags verified against vendored KRT 0.19.0 source.
  Files: route_critical.py (+370 lines), test_route_critical.py (+7 tests).
- P7A-4==P7B-4 drill-aware hole floors: geom.Pad gains drill/(w,h) + drill_offset + drill_poly
  (slot stadium, -prot rotation trap honored per LEARNINGS:827); stitch_vias hole_ok
  edge-to-edge (EDGE_GAP 0.2 + new drill radius == S11 0.5mm floor for standard 0.3 drills,
  stricter only above); plane_repair same. DEVIATED: EDGE_GAP stays constant rather than
  sourcing fabfloors min_hole_to_hole (no fab profile plumbed into stitch_vias; semantics
  unverified). Files: geom.py, stitch_vias.py, plane_repair.py; +4 tests incl. rotated slot.
- P7A-5 planes_gen: grid points dropped inside hole floor of existing EP drills; >=4 existing
  thru-hole items skips grid with fact note; _rects_overlap <= so touching rects get distinct
  priorities. +5 tests.
- P7B-1 route_critical --pad-window probe (widest connectable track per power pad, shape-aware,
  unnetted pads counted foreign, exit 1 on DRU floor unmeetable). DEVIATED: continuous
  bisection solver, not the eval's 0.025 grid; known answer pinned 1.489mm not the eval's
  1.465 +/- 0.01 (LEARNINGS 782 hand value irreproducible: faithful grid reimpl gives 1.396;
  both methods agree 1.75 floor unmeetable, which is what gates). test_pad_window.py new, 6 tests.
- P7B-2 planes_gen sidecar "connect": "solid"|"thermal" -> ZONE_CONNECTION_FULL via route_swig;
  live smoke shows "(connect_pads yes" in saved zone. Hand-edit contradiction gone.
- P7B-3 router.md step 5b 8->3 lines (remediation pointer) + 2-line --pad-window premise hook;
  net 75 -> 72 lines. DEVIATED: one over the <=71 target (target predates the hook's +2 lines).
  track_width.md step 4 names the probe + connect:solid invocation.
- Adjacent fix (unplanned): grading_floors read nonexistent .kicad_pro key "min_copper_to_edge"
  vs KiCad's "min_copper_edge_clearance" - real edge floor never loaded, KRT got stock 0.5.
  Behavior change on live runs (KRT now gets e.g. 0.3 on T1-hardened boards); smoke green.

Corrections (eval claims refuted during apply):
- V13 root cause is the DANGLING pass, not the loop-breaker - PROGRESS.md:101 register wording
  stands corrected; fix landed and live-verified on the fixture.
- P7B-6 (check_diffpair same-footprint terminal pairing) was ALREADY DONE in T2 commit 46900a0:
  check_diffpair.py:58 "cross-ref matched-terminal window (same-ref: none)". Eval flagged it
  open against a stale read; no change needed. Carrier RX-skew H4 board question unaffected.
- P7B-1 known-answer value 1.465 unreproducible by either solver method (see above).

Tests: batch files 152 pure + 18 smoke passed (route_auto smoke run ALONE per LEARNINGS:2202,
no flake); full repo 951 passed -m "not smoke and not net". Pre-existing unrelated net-marked
parts test failure flagged to Batch C owner.

## No change
- lib/routelib.py, lib/route_swig.py core, route_edit.py atomic stage/apply/verify - verified
  against LEARNINGS:416; live-proven; dedup_copper firing confirmed (pd-trigger 3 echoes).
- route_auto ladder/wedge-detect/KRT-finish core - rung 1 sufficed on both 4L boards; wedge
  never re-fired since S11 mitigation.
- lib/netconn.py - read in full; join rules match machine-verified KiCad facts; correct
  foundation for the P7A-1 bridge veto.
- router.md chain orders (11-17), placement_adjust_request protocol, snapshot discipline,
  output contract, V13 skip guidance (kept until live proof), freerouting_retries budget.
- Bench P7 weights: via_count penalizes deliberate stitch hygiene and track_mm is blind to pour
  fan-in copper - recorded as fairness nuance, NOT changed (re-baseline churn not worth it
  while nothing is scored on stitch density).

## Deferred / rejected
Spec for all deferred items lives in the two P7 eval JSONs (durable record).
- P7A-6 route_probe listing WHICH connections stayed unrouted - ladder row 76 stays open.
- P7A-7 DSN post-process assertion set unbuilt - V14 disposition: mitigated at runtime
  (wedge-detect + KRT fallback), assert set unbuilt, upstream repro worth filing.
- P7A-8 power-width feasibility at rules_gen time + auto pour-lobe generation - cross-stage
  (P5/P7 contract), L size; measurement half now exists as --pad-window. Rows 78 residual /
  82 open.
- P7B-7 chain order -> T10 task recipes (row 50 already planned-T10).
- P7B-8 viasless pour-channel current measurement - P8 check_current owner; pd-trigger frozen
  board is the ready-made fixture (channels 1.11/1.25mm invisible to every checker).
Ladder rows updated by Batch G: 78 (L0->L2), 81 (->L3), 82 (note, open), 120/121/127/131/132
(->L3), 144 (->L3).

## Bench
fixture                     before   after   note
golden_usbbuck4_route (P7)  92.54    92.54   delta 0.0; headroom is track_mm/via_count only
pd_trigger_route (P7)       91.50    91.50   delta 0.0
mutant_planesplit_verify    99.40    99.40   collateral (geom.py shared); known_answer 1
mutant_returnvia_verify     99.50    99.50   known_answer 1
mutant_gndchoke_verify      94.80    94.80   known_answer 2
carrier_verify (P8)         68.20    68.20   known_answer 8
pd_trigger_verify (P8)      94.40    94.40   delta 0.0
mutant_cpl_dfm (P9)         99.40    99.40   mutant catching intact
pd_trigger_dfm (P9)         98.00    98.00   delta 0.0
Baselines re-recorded: none. Batch bench-neutral by construction (P7 fixtures score frozen
final boards; no check semantics changed for scored fixtures). Caveat: --net-clearances has no
live KRT run with a non-empty map yet - first carrier-class board exercises it.

## Cost & tiers
Tokens/run: clean chain ~100-150k unchanged; failure tail ~400k-1M -> ~200-400k (P7A-3 turns
the manual KRT loop into one scripted retry; P7A-1 removes cleanup restore-churn; P7B-1 kills
the in-context pad-geometry derivation, est. 30-60k/complex board; P7B-3 ~100 tok/spawn).
Model/effort (lenses diverge; B's is grounded in run evidence and stands):
- router, novel/complex board: fable max (B, CONFIRMED - judgment pivots are board-critical;
  A argued medium suffices since scripts do the search; keep max until a downgrade is benched).
- router, re-run of proven chain / simple 2L: sonnet high as EXPERIMENT ONLY now that
  P7B-1/P7A-3 landed - blinky's marginal-fix refusal is weak-model failure territory.
- router, KRT long-haul fix loop: was fable high; collapses into the default now that P7A-3
  encodes the escalation.
