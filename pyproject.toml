[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
markers = [
    "smoke: live-toolchain smoke tests (need KiCad installed; slower)",
    "net: live-network tests (hit the JLCPCB/EasyEDA APIs; need internet)",
]
