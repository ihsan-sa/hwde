learning block-basics: a single-channel analog-to-digital converter. Analog
input 0-5 V DC on a screw terminal; 12-bit or better; up to 10 kSa/s; DC
accuracy matters more than speed. Digital output to a host over a 0.1 inch
header, which also feeds the board its 3.3 V rail. Workspace boards/bb-adc.

---

Owner brief, verbatim, received 2026-08-16. The text above is the owner's own
wording and is the authoritative statement of the task; nothing below it is
part of the brief.

Mode token resolved at P0 by `state.py mode`:

- token: `learning block-basics:`
- target: `block-basics` -> scope tier `block-only`, binding `canonical`
- geometry: OUTPUT (the placement earns the outline; `board_init --outline auto`)
- stage under study: none
