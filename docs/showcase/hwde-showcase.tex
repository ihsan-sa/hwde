% The hwde showcase. Build with ./build_docs.sh (pdflatex, twice).
\documentclass[11pt,a4paper]{article}

\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{helvet}\renewcommand{\familydefault}{\sfdefault}
\usepackage[dvipsnames]{xcolor}
\usepackage[margin=18mm,top=16mm,bottom=16mm]{geometry}
\usepackage{graphicx}
\usepackage{titlesec}
\usepackage{array}
\usepackage{booktabs}
\usepackage{enumitem}
\usepackage{ragged2e}
\usepackage[hidelinks]{hyperref}

% Same palette as the flow charts.
\definecolor{gold}{HTML}{B8943E}
\definecolor{muted}{HTML}{9C988F}
\definecolor{ink}{HTML}{3A3833}
\definecolor{surface}{HTML}{F4F1EB}
\definecolor{canvas}{HTML}{FAF9F6}
\definecolor{border}{HTML}{E8E4DC}
\definecolor{accent}{HTML}{C96442}
\definecolor{lblue}{HTML}{3E6C8F}
\definecolor{lgreen}{HTML}{4F7A52}

\pagecolor{canvas}          % the renders are composited onto this exact colour
\color{ink}

\graphicspath{{renders/}{diagrams/}}
\setlength{\parindent}{0pt}
\setlength{\parskip}{6pt}
\pagestyle{empty}

\titleformat{\section}{\Large\bfseries\color{gold}}{}{0pt}{}
\titlespacing*{\section}{0pt}{14pt}{6pt}
\titleformat{\subsection}{\large\bfseries\color{ink}}{}{0pt}{}
\titlespacing*{\subsection}{0pt}{10pt}{2pt}

% --- helpers -------------------------------------------------------------
\newcommand{\lead}[1]{{\large\color{ink}#1\par}}
\newcommand{\note}[1]{{\small\color{muted}#1\par}}

% One board, full width: render then caption. The whole thing is one minipage so
% a caption can never be orphaned onto the next page from its render.
% #1 image  #2 name  #3 what it is  #4 the record
\newcommand{\board}[4]{%
  \par\vspace{4pt}%
  \noindent\begin{minipage}{\linewidth}
    \setlength{\parskip}{0pt}\setlength{\parindent}{0pt}%
    \centering
    \includegraphics[width=\linewidth,height=0.355\textheight,keepaspectratio]{#1}

    \vspace{4pt}
    \raggedright
    {\large\bfseries\color{lblue}#2}\quad{\color{muted}\small #4}

    \vspace{2pt}
    \justifying\noindent #3
  \end{minipage}\par
}

% One board, half width, for the small ones. The render sits in a fixed-height
% box so two side by side have their captions on the same line.
\newcommand{\halfimg}[2]{\parbox[t][#1][c]{\linewidth}{%
  \centering\includegraphics[width=\linewidth,height=#1,keepaspectratio]{#2}}}
\newcommand{\boardhalf}[4]{%
  \begin{minipage}[t]{0.48\linewidth}
    \setlength{\parskip}{0pt}\setlength{\parindent}{0pt}%
    \halfimg{46mm}{#1}

    \vspace{4pt}
    {\bfseries\color{lblue}#2}

    \vspace{1pt}
    {\scriptsize\color{muted}#4}

    \vspace{3pt}
    {\small\justifying\noindent #3\par}
  \end{minipage}%
}

\newcommand{\chart}[1]{%
  \par\vspace{2pt}\noindent%
  \includegraphics[width=\linewidth]{#1}\par\vspace{2pt}%
}

\begin{document}

% ======================================================================
% Title
% ======================================================================
\begin{center}
  \vspace*{2mm}
  {\fontsize{46}{50}\selectfont\bfseries\color{gold}hwde}\par
  \vspace{3mm}
  {\Large\color{ink}An AI PCB engineer for KiCad and JLCPCB}\par
  \vspace{1mm}
  {\color{muted}printed circuit boards, designed from a written brief}\par
\end{center}

\vspace{4mm}

\begin{center}
\begin{minipage}{0.86\linewidth}
\lead{You describe the board you want in plain words. hwde then researches the
parts, picks a topology, draws the schematic, and lays out and routes the
board. It runs the electrical and manufacturing checks, and hands you the files
a factory needs to build it. A human engineer signs off at every stage.}
\end{minipage}
\end{center}

\vspace{5mm}

\begin{center}
\includegraphics[width=\linewidth,height=0.50\textheight,keepaspectratio]{lumina-carrier}
\end{center}

\vspace{1mm}
\begin{center}
\begin{minipage}{0.86\linewidth}
\centering
\note{\textbf{lumina-carrier} is a 4-layer carrier board for a stage-lighting
fixture, 100 $\times$ 80\,mm with 116 footprints. hwde designed it, and it was
ordered from JLCPCB and fabricated.}
\end{minipage}
\end{center}

\newpage

% ======================================================================
% How it works
% ======================================================================
\section{How it works}

Three pictures follow. The first is the route a board takes, the second is who
does which part of the work, and the third is what happens at every handover.

\subsection{The pipeline}

\chart{pipeline}

A run starts from a written brief and ends with a manufacturing package of
Gerbers, a bill of materials and a pick-and-place file. Each stage in between
produces one file, and before it does, it reads the knowledge base of design
rules, datasheet numbers and what the factory can really make. The orange dots
mark where a person has to look at the work and agree before it goes on.

\subsection{Who does what}

\chart{roles}

The work is split in two. Twenty-two specialist subagents make the judgement
calls, like choosing a topology, sizing a part or reviewing a schematic, and
each one has a written contract for what it may decide. About sixty scripts do
and check the mechanical work, and they are all deterministic. They drive KiCad,
run the routers and export the Gerbers. A script never has an opinion, and an
agent never edits a board file by hand. Whatever either of them produces lands
as a file in one folder per board.

\newpage

\subsection{The gate loop}

\chart{gate}

Every stage ends at a gate. The gate runs the checks that apply to that stage,
which are the electrical rules, the design rules, manufacturability and circuit
simulations against stated bounds. It either passes, or it hands the findings
back to a fix loop that reworks the file and tries again. A pass is not the end
of it, because a person still signs off before the next stage starts. A green
gate only proves the checks that actually ran, so the reports are kept one per
check instead of being collapsed into a verdict.

% ======================================================================
% The boards
% ======================================================================
\section{The boards}

Fourteen boards got far enough to have a laid-out, routed PCB. They run from a
linear regulator with five parts to a 200\,W RF power stage. Every render below
was made from the board file in the repository, by the script that sits next to
this document.

\vspace{2mm}

{\small
\begin{tabular}{@{}l l r r l@{}}
\toprule
\textbf{Board} & \textbf{What it is} & \textbf{Layers} & \textbf{Size (mm)} & \textbf{Stage reached}\\
\midrule
lumina-carrier & lighting carrier, PoE-powered & 4 & 100 $\times$ 80 & ordered, fabricated\\
pd-trigger     & USB-C PD bench trigger        & 2 & 48 $\times$ 30   & ordered, fabricated\\
lumina-par     & RGBW PAR daughter board       & 4 & 100 $\times$ 80 & verification\\
rf-de-20m      & 20 MHz Class E GaN stage      & 4 & 120 $\times$ 80 & manufacturability\\
sbuck-5v3a     & 5 V / 3 A synchronous buck    & 4 & 50 $\times$ 40   & package ready\\
usb-buck       & USB device dev board          & 4 & 50 $\times$ 40   & package ready\\
g0-sense       & USB-C sensor node             & 2 & 35.8 $\times$ 28.3 & package ready\\
stm32-blinky   & minimal STM32 dev board       & 2 & 50 $\times$ 40   & package ready\\
rf-term-150w   & 150 W RF dummy load head      & 2 & 26 $\times$ 20   & manufacturability\\
bb-adc         & single-channel ADC            & 2 & 54.8 $\times$ 34.9 & manufacturability\\
bb-amp         & sensor amplifier chain        & 2 & 48 $\times$ 28.3 & manufacturability\\
bb-buck        & bare buck converter           & 2 & 35 $\times$ 25   & manufacturability\\
bb-ldo         & bare linear regulator         & 2 & 34.7 $\times$ 34.7 & manufacturability\\
bb-mcu         & bare microcontroller board    & 2 & 34.8 $\times$ 22.3 & manufacturability\\
\bottomrule
\end{tabular}}

\vspace{3mm}

\note{``Stage reached'' says where the board stopped in the pipeline, and it does
not say the board is fit to release. Two boards say ``ordered, fabricated''
because their own files record a JLCPCB order number that tracked through to
shipped. The rest stopped with a checked manufacturing package in hand. Each
size is the bounding box of the board outline, and not every outline is a
rectangle.}

\newpage

\subsection{Designed, ordered and fabricated}

\board{lumina-carrier}{lumina-carrier}
{This is the universal carrier for a stage-lighting fixture. It takes power over
Ethernet through a PD controller and a 100\,V buck, and it carries a frozen
expansion connector that a fixture-specific daughter board plugs into. It is the
biggest board here, with 4 layers and 116 footprints, and another board depends
on the connector it sets.}
{4 layers \; 100 $\times$ 80 mm \; 116 footprints \; ordered from JLCPCB, tracked to shipped}

\vspace{2mm}

\board{pd-trigger}{pd-trigger}
{This one is a bench tool. A USB-C Power Delivery sink controller negotiates
with a charger for 5, 9, 12, 15 or 20 volts, and you pick the profile on the
board. The negotiated rail comes out on a screw terminal rated for 5\,A, so up to
100\,W. The board converts nothing. It just passes the rail through.}
{2 layers \; 48 $\times$ 30 mm \; 30 footprints \; ordered from JLCPCB, tracked to shipped}

\newpage

\subsection{Harder boards}

\board{rf-de-20m}{rf-de-20m}
{A 20\,MHz Class E RF power stage that puts 200\,W into 50 ohms from a 40\,V
bus. A PWM drive arrives on an SMA input, a GaN gate driver buffers it, and one
ground-referenced eGaN FET switches. A resonant tank and an L-match transform
the 4.6-ohm load line to 50 ohms at the output SMA. The owner fixed the topology
and the two core parts after a costed trade study. hwde did the rest.}
{4 layers \; 120 $\times$ 80 mm \; 80 footprints \; stopped at manufacturability checks}

\vspace{2mm}

\board{lumina-par}{lumina-par}
{The RGBW PAR daughter board that stacks on the carrier. The carrier's connector
document is frozen, so this board takes it as given and may not redefine it. Its
power budget of 8.6 to 20\,W was re-derived here, and depends on the
power-over-Ethernet class the fixture gets. It is the densest board here at 158
footprints, and the only one whose outline is not a rectangle. The silkscreen
warns that the board floats at the power-over-Ethernet potential, because an
earthed probe breaks the fixture's power negotiation.}
{4 layers \; 100 $\times$ 80 mm \; 158 footprints \; stopped at verification}

\newpage

\boardhalf{lumina-par-flat}{lumina-par, straight down}
{The same board from straight above, so the routing shows. It has four layers
and 158 footprints, routed to the same rules as everything else.}
{top-down view}
\hfill
\boardhalf{lumina-carrier-bottom}{lumina-carrier, underneath}
{Every part sits on the top side of this board, so the back carries routing and
vias and nothing else. This is how the factory sees it.}
{bottom view}

\vspace{4mm}

\subsection{Power}

\boardhalf{sbuck-5v3a}{sbuck-5v3a}
{A synchronous buck that takes 7--18\,V in and holds 5.0\,V $\pm$2\% at 3\,A
out. It is an open-frame power module.}
{4 layers \; 50 $\times$ 40 mm \; 44 footprints \; package ready}
\hfill
\boardhalf{rf-term-150w}{rf-term-150w}
{A 50-ohm, 150\,W dummy load head for DC to 25\,MHz. The element bolts to a
heatsink the user supplies, so the board is only the RF launch and the
mechanical interface. It renders close to bare because none of its six footprints has a 3D model
the renderer could find, so the connector and the element are not drawn.}
{2 layers \; 26 $\times$ 20 mm \; 6 footprints \; stopped at manufacturability checks}

\newpage

\subsection{Microcontroller boards}

\board{g0-sense}{g0-sense}
{A temperature and humidity node that runs off USB-C. USB-C supplies 5\,V and an
LDO makes 3.3\,V from it, and an STM32G030 on its internal oscillator reads a
Sensirion SHT4x over I\textsuperscript{2}C. The same bus comes out on a Qwiic
connector, the readings go out over a UART header, and you program it over SWD.
hwde designed this one end to end in a single unattended container run.}
{2 layers \; 35.8 $\times$ 28.3 mm \; 30 footprints \; package ready, release attestation built}

\vspace{2mm}

\boardhalf{usb-buck}{usb-buck}
{An STM32F103 board that talks USB at full speed. The Micro-B socket carries
both power and data, and there is an LED, a button and an SWD header.}
{4 layers \; 50 $\times$ 40 mm \; 28 footprints \; package ready}
\hfill
\boardhalf{stm32-blinky}{stm32-blinky}
{The simplest useful board here. It has an STM32F103, an 8\,MHz crystal, one
LED, an LDO and an SWD header.}
{2 layers \; 50 $\times$ 40 mm \; 20 footprints \; package ready}

\newpage

\subsection{Building blocks}

These five boards are deliberately bare, each one carrying a single block and
nothing else. They are there to be studied and measured on a bench, so they
leave out protection, filtering, indicators and spare rails on purpose.

\vspace{2mm}

\boardhalf{bb-buck}{bb-buck}
{It takes 18--30\,V in and gives 5\,V at 2\,A out on a screw terminal. It
carries the converter and exactly what its datasheet requires.}
{2 layers \; 35 $\times$ 25 mm \; 20 footprints}
\hfill
\boardhalf{bb-ldo}{bb-ldo}
{It takes 5\,V in and gives 3.3\,V at 500\,mA out, holding regulation in still
air on the board's own copper.}
{2 layers \; 34.7 $\times$ 34.7 mm \; 5 footprints}

\vspace{4mm}

\boardhalf{bb-mcu}{bb-mcu}
{One MCU with a 3.3\,V rail coming in, its debug header and four GPIO. There is
no regulator and no second rail.}
{2 layers \; 34.8 $\times$ 22.3 mm \; 14 footprints}
\hfill
\boardhalf{bb-adc}{bb-adc}
{0--5\,V in on a screw terminal, and 12 bits or better at up to 10\,kSa/s out
over a 0.1-inch header. It trades speed for DC accuracy.}
{2 layers \; 54.8 $\times$ 34.9 mm \; 25 footprints}

\vspace{4mm}

\boardhalf{bb-amp}{bb-amp}
{It takes a 0--20\,mV differential bridge signal and gives 0--3.3\,V
single-ended out, from DC to 1\,kHz. The nominal gain is 165 V/V.}
{2 layers \; 48 $\times$ 28.3 mm \; 14 footprints}
\hfill
\begin{minipage}[t]{0.48\linewidth}
\vspace{6mm}
\note{Two more boards stopped before layout and have no PCB to show, a
5\,V/3\,A buck and a strobe daughter board. Both are in the repository at the
stage they reached.}
\end{minipage}

\newpage

% ======================================================================
% Honest limits
% ======================================================================
\section{What it can and cannot do yet}

hwde is an engineering assistant, and it does not release a board by itself. A
human engineer drives it and signs off at every stage. Two of the boards above
were designed with it, ordered from a factory and fabricated.

\subsection{What holds}

\begin{itemize}[leftmargin=5mm,itemsep=2pt]
\item Fourteen boards went from a written brief to a routed layout, including a
      4-layer 200\,W RF stage and a novel 100\,W USB-C PD design.
\item Two of them were ordered from a factory and fabricated, and their own
      files record the order numbers tracked through to shipped.
\item Every check is a script, and they all follow the same contract. The
      findings are kept per check rather than as a single verdict.
\item Everything a run produces is a file in that board's folder, and you can
      read it without hwde.
\end{itemize}

\subsection{What does not}

\begin{itemize}[leftmargin=5mm,itemsep=2pt]
\item A green gate only proves the checks that \emph{ran}. Read the report for
      each check rather than the verdict.
\item The stage a board reached is not a release certificate. A person decides
      what is fit to release.
\item The factory has no public API for its manufacturability review, so someone
      still does that step by hand in a browser.
\item Spending money is deliberately hard. One code path can place an order, and
      it handles 2-layer boards only. It refuses a board whose files already
      record an order, and it will not run without a fresh quote and a matching
      design hash. A person also has to type out a confirmation naming the
      board, the quantity and the total.
\item The electrical and thermal checks are engineering screens, and each states
      its own accuracy. None of them is a certification, and nothing here is
      safety-certified for mains, medical, automotive or aerospace use.
\end{itemize}

\vspace{4mm}

\section{How it gets better}

\chart{lessons}

Every run writes down what broke and why, with a date and a tag, and that goes
into the knowledge base the next run reads first. The rule is to record a gotcha
the moment you hit it, so the next board does not pay for it again.

\vfill
\note{The renders come from the board files in this repository, drawn by KiCad's
command-line renderer. The dimensions, layer counts and footprint counts are
read from those same files, and the stage each board reached is read from its
recorded state.}

\end{document}
